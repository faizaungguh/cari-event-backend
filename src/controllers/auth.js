const adminLogin = () => {
  console.log('ini dari Auth Admin untuk Login');
};

const creatorRegister = () => {
  console.log('ini dari Auth untuk register');
};

const creatorLogin = () => {
  console.log('ini dari Auth untuk login');
};

const customerRegister = () => {
  console.log('ini dari Auth untuk register');
};

const customerLogin = () => {
  console.log('ini dari Auth untuk login');
};

module.exports = {
  adminLogin,
  creatorRegister,
  creatorLogin,
  customerRegister,
  customerLogin,
};
